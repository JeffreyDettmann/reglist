<!DOCTYPE html>
<html>
  <head>
    <title>Reglist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
	<div class="container">
	  <div class="banner row mt-5">
		<h1 class="col-11">Age of Empires Tournament Registration Opportunities</h1>
		<div class="col-1">
          <%= image_tag('profile_800x800.png', width: 80, class: 'rounded', alt: :logo) %>
		  </div>
	  </div>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
	  <nav class="navbar">
		<div class="container-fluid">
		  <div class="navbar-nav ms-auto">
			<%= link_to 'Home', root_url %>
		  </div>
		  <div class="navbar-nav ms-auto">
			<%= link_to 'Manage tournaments', admin_tournaments_path %>
		  </div>
		  <div class="navbar-nav ms-auto">
			<%= button_to('Sign Out', destroy_user_session_path, method: :delete) if current_user %>
		  </div>
		</div>
	  </nav>
	</div>
  </body>
</html>
