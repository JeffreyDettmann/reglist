<!DOCTYPE html>
<html>
  <head>
    <title>Reglist</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
	<div class="container">
	  <div class="banner rounded row mt-5">
		<h1 class="col-11 text-center">Age of Empires Tournament Registration Opportunities</h1>
		<div class="col-1 mt-2">
		  <a data-bs-toggle="collapse" href="#bottom-navbar">
			<%= image_tag('profile_800x800.png', width: 80, class: 'rounded', alt: :logo) %>
		  </a>
		</div>
	  </div>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
	  <nav class="navbar <%= 'collapse' unless current_user %>" id="bottom-navbar">
		  <div>
			<%= link_to 'Home', root_url  if current_user %>
		  </div>
		  <div>
			<%= link_to 'Manage tournaments', admin_tournaments_path %>
		  </div>
		  <div>
			<%= button_to('Sign Out', destroy_user_session_path, method: :delete) if current_user %>
		  </div>
	  </nav>
	</div>
  </body>
</html>
