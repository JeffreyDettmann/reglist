<!DOCTYPE html>
<html>
  <head>
    <title>Tourney Opportunities</title>
    <meta charset="utf-8" /> 
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag asset_path('favicon.ico') %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
	<div class="container">
	  <div class="banner rounded row mt-5 mb-5">
		<h1 class="col-10 col-lg-11 text-lg-center pt-2">Age of Empires Tournament Registration Opportunities</h1>
		<div class="col-2 col-lg-1 mt-2 mb-2">
		  <a data-bs-toggle="collapse" href="#bottom-navbar" class="float-end">
			<%= image_tag('profile_800x800.png', width: 80, class: 'rounded', alt: :logo) %>
		  </a>
		</div>
	  </div>
      <% if notice %><p class="alert alert-success"><%= notice %></p><% end %>
      <% if alert %><p class="alert alert-danger"><%= alert %></p><% end %>
      <%= yield %>
      <div>Have something you'd like to tell us? <%= link_to 'Send us a message!', new_messages_path %></div>
	  <nav class="navbar <%= 'collapse' unless current_user %>" id="bottom-navbar">
		  <div>
			<%= link_to 'Home', root_url  if current_user %>
		  </div>
		  <div>
			<%= link_to 'Manage tournaments', admin_tournaments_path %>
		  </div>
		  <div>
			<%= link_to 'Messages', admin_messages_url  if current_user %>
		  </div>
		  <div>
			<%= button_to('Sign Out', destroy_user_session_path, method: :delete) if current_user %>
		  </div>
	  </nav>
	</div>
  </body>
</html>
